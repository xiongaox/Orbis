这是一份为您定制的**八字模块接入指令文档**。文档严格遵循您的要求，仅使用 lunar-python 作为算法核心，旨在指导 AI 将您的 React 静态前端通过 FastAPI 接入动态八字计算逻辑。

# ---

**八字模块接入指令文档（AI 专用版）**

## **1\. 项目架构与技术目标**

* **前端**：React \+ TypeScript \+ Vite（目前为静态 UI）。  
* **后端**：Python (FastAPI)，采用 Vercel Serverless 混合部署架构。  
* **核心算法库**：**lunar-python**（必须使用此库处理所有历法与八字逻辑）。  
* **目标**：实现从前端输入公历/农历时间，后端通过 lunar-python 计算精准八字数据（四柱、十神、藏干、大运等），并返回 JSON 供前端渲染。

## **2\. 后端实现规范 (Python / FastAPI)**

### **A. 依赖安装**

在 api/requirements.txt 中添加：

Plaintext

fastapi  
lunar-python  
uvicorn

### **B. API 接口逻辑设计**

在 api/main.py 中实现 /api/bazi 接口，要求 AI 执行以下步骤：

1. **解析输入**：接收前端传来的 year, month, day, hour, minute 参数。  
2. **历法转换**：使用 Solar.fromYmdHms() 创建公历对象，并获取对应的 Lunar 对象。  
3. **提取八字 (EightChar)**：  
   * 通过 lunar.getEightChar() 获取八字核心对象。  
   * 获取 **四柱干支**（年、月、日、时柱）。  
   * 获取 **藏干** 与 **地支余气**。  
   * 获取 **十神**（根据日主天干计算其他七字的十神关系）。  
4. **运势计算**：  
   * 获取 **起运时间**（几年几个月几天）。  
   * 获取 **大运列表**（各阶段起运年龄、干支）。  
5. **数据封装**：将上述所有数据封装为标准的字典（Dictionary）格式返回给前端。

## **3\. 前端 TypeScript 接入规范**

### **A. 定义数据契约 (src/types/bazi.ts)**

要求 AI 根据 lunar-python 的输出结构定义 TS 接口，例如：

TypeScript

export interface BaziData {  
  solarDate: string;    // 公历日期描述  
  lunarDate: string;    // 农历日期描述  
  pillars: {  
    year: string;       // 年柱 (如: 庚午)  
    month: string;      // 月柱  
    day: string;        // 日柱  
    hour: string;       // 时柱  
  };  
  tenGods: string\[\];    // 十神列表  
  hiddenStems: string\[\]\[\]; // 各地支藏干  
  daYun: {  
    startYear: number;  // 起运年份  
    cycles: Array\<{ age: number; ganZhi: string }\>; // 大运周期  
  };  
  // ... 其他必要字段  
}

### **B. 接入逻辑**

1. **创建 API Service**：编写异步函数 fetchBazi(params) 访问 /api/bazi。  
2. **替换静态状态**：  
   * 在主排盘组件中使用 useState\<BaziData | null\>(null)。  
   * 调用接口后更新状态，并将 baziData 传递给现有的展示组件。

## **4\. 给 AI 的具体 Prompt（直接粘贴给 AI）**

“我有一个基于 React \+ TS 的静态八字排盘前端。现在我需要接入真实的后端计算逻辑。

**请执行以下任务：**

1. 在 api/main.py 中使用 **FastAPI** 编写一个 /api/bazi 接口。  
2. 后端必须使用 **lunar-python** 库。请通过前端传入的年月日时分信息，计算出完整的八字（四柱、十神、地支藏干）、起运时间、以及大运列表。  
3. 根据 lunar-python 的计算结果，为我定义一套匹配的 TypeScript 接口（Interface）。  
4. 修改我的前端逻辑，将目前静态的 Mock 数据替换为从该 API 获取的动态数据，并确保 UI 渲染正确。

请先从后端 API 的核心逻辑实现开始。”

### ---

**文档使用建议：**

* **保持简单**：这份文档排除了奇门等干扰项，AI 会专注于 lunar-python 的对象模型（如 EightChar、EightChar.getTenVar() 等方法）。  
* **精准对齐**：由于 lunar-python 的 API 非常标准，AI 生成的代码通常不需要大幅修改即可运行。